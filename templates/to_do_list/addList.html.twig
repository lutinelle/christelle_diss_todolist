{% extends 'base.html.twig' %}
{% block body %}

    <div class="form">

                    <h5 class="title" >Creer liste</h5>


                {{ form_start (formList) }}
                <div class="m-3">
                {{ form_row(formList.name) }}
                </div>
                <div class="footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                    <button type="submit" class="btn btn-primary">Creer</button>
                </div>
                {{ form_end (formList) }}
    </div>

{% endblock %}