{% extends 'base.html.twig' %}
{% block body %}

    <div class="form">
                    <h5 class="modal-title" id="exampleModalLabel">Renommer la liste</h5>

                {{ form_start (formList) }}
                <div class="m-3">
                    <label for="exampleFormControlInput1" class="form-label">Nouveau nom de la liste</label>
                    {{ form_widget(formList.name) }}
                </div>
                <div class="footer">

                    <a href="{{ path('index') }}" class="btn btn-secondary">Annuler</a>
                    <button type="submit" class="btn btn-primary">Renommer</button>
                </div>
                {{ form_end (formList) }}


{% endblock %}